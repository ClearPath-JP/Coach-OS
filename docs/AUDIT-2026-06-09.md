# Ready-To-Go Night Audit — 2026-06-09

Full-app criticism pass before charging coach #1. Sources: 3 code auditors (money path, coach surfaces, client portal), live Playwright walkthrough (desktop + real 375px), Supabase advisors. Rubric per `docs/claude-chrome-audit.md`: brass `#c8882e` only, Shippori Mincho + Instrument Sans, honest copy, 🔴 broken / 🟡 fix tonight / 🟢 solid / 💡 later.

Status: IN PROGRESS — sections land as each auditor reports.

---

## Supabase advisors (checked first, prod project owiqourfyjxwveopijrg)

- 🟢 Security: the 7 `SECURITY DEFINER` warnings are the accepted baseline — `get_coach_*` RPCs self-authorize via `auth.uid()` (verified in `20260402003000` + `20260402001000`), `is_super_admin()`/`current_workspace_id()` intentionally granted for RLS (`20260607120000_harden_security_definer_functions.sql`).
- 🟡 OWNER GATE (known): leaked-password protection requires Supabase Pro.
- Performance advisor dump (289KB) → digest pending (background agent).

---

## CLIENT PORTAL (agent report, code-verified)

Verified healthy: `--cp-accent` separation correct (`app/client/layout.tsx:62-63`, zero brass literals in client surfaces), no Kindo/Sensei strings, no native alert/confirm, pass webhook idempotent (`lib/stripe-pass-purchase-webhook.ts:29-36`), shared-video access workspace-gated (`lib/video-stream-access.ts:44-57`), real empty states throughout.

### 🔴 Broken
1. **Covered bookings (pass/membership) succeed but client sees an error; credit silently spent.** `app/client/(main)/classes/ClientClassesContent.tsx:100-122` vs `app/api/client/book-class/route.ts:263,368` — server returns `{booked:true, passUsed:true}` with no `url`; client only handles `data.url` → falls to `setBookError('Booking link missing')`, list never reloads, retry → 409 "already booked". Fix: handle `booked===true` → success banner + `load()`. **S**
2. **No mobile path to /client/classes — credits can be bought but never spent on a phone.** Only link = desktop sidebar (`components/layout/ClientPortalSidebar.tsx:23`); absent from `MobileNav.tsx:172-180` and portal quick-nav grid (`app/client/(main)/portal/page.tsx:264-271`); Passes page says "spend one each time you book" (`PassesContent.tsx:148`) without linking. Fix: add "Book a class" to portal quick-nav + CTA on Passes page. **S**

### 🟡 Fix tonight
3. **Paid class booking lands on /client/sessions with zero acknowledgment + double-pay window.** `book-class/route.ts:416-417` redirects `?booked=1` to sessions, but `ClientSessionsContent.tsx` never reads it (banner only exists in ClientClassesContent.tsx:36-51). Webhook lag → booking not visible; dup guard only checks *paid* rows (`book-class/route.ts:109-116`) → re-book = second live checkout. Fix: read `booked`/`booking_cancelled` params on sessions page. **S**
4. **Passes page never refetches after purchase** — `PassesContent.tsx:44-75,151-156` loads once on mount, races webhook; success banner sits above "0 credits". Fix: poll on `purchased=1` (~2s × 5) or Refresh button. **S**
5. **Booking coverage opaque: "Book & pay $25" shown to members/pass-holders; members-only classes 403 only after tap.** `ClientClassesContent.tsx:236-240,258-266`; API never returns `member_access`/coverage (`app/api/client/available-classes/route.ts:62-74`). Fix: return computed `willUse: membership|pass|money` + render "Covered by membership" / "Uses 1 credit · N left" + members-only badge. **M**
6. **Expired pass credits display as spendable, then booking charges money instead.** `app/api/client/passes/route.ts:41-47` ignores `expires_at` (nothing flips status to `expired`), but spend-time selection excludes expired (`lib/pass-selection.ts:26`). Fix: filter balances by expiry (+optional greyed Expired section). **S**
7. **Portal home date + greeting use server UTC clock** — `app/client/(main)/portal/page.tsx:179` (`new Date()` in server component) → lines 228/310/312 (`portalGreetingLine`, `lib/portal-time-greeting.ts:3`). Evening student sees tomorrow's date + "Good morning". Same class as 4bc7975, other surface. Fix: neutral SSR + client-mount compute (4bc7975 pattern). **S**

### 💡 Later
8. Desktop video modal capped at 448px (`ClientVideosContent.tsx:126` override loses to Modal base `md:max-w-md`, `components/ui/Modal.tsx:104`) — add `md:max-w-none`. **S**
9. Coach-theme `--accent-light`/`--accent-muted` (pale crimson in light mode, `app/globals.css:83`) behind client content: `ClientPortalSidebar.tsx:55,88`, `ClientSessionsContent.tsx:112,116`, `ClientInvoicesList.tsx:136`, `MobileNav.tsx:326`. Swap to `--cp-accent-light` (`globals.css:233`). **S**
10. `formatCents` hardcodes USD (`lib/format-currency.ts:1-3`) used by passes/classes/membership prices; invoices do it right (`ClientInvoicesList.tsx:40-45`). Add currency param. **S**
11. 7 mobile tabs at 375px ellipsize "Membership" (`MobileNav.tsx:172-180`, label clamp 339-346). Shorter label or consolidate (interacts with finding 2). **S**
12. "I've sent payment" chat message embeds raw invoice UUID (`ClientInvoicesList.tsx:186`). Use amount + package title. **S**
13. Invoice card-pay redirect skips the `checkout.stripe.com` allowlist guard others use (`ClientInvoicesList.tsx:122-126` vs `PassesContent.tsx:95-100`). **S**
14. Dead SSR-hazard components `ClientWeekCalendar`/`ClientSessionsMonthCalendar` (`components/client/ClientWeekCalendar.tsx:46-47`) — delete. **S**

**Agent's top 3:** #1 (success-shown-as-error at the money moment), #2 (mobile spend path), #3+#4 (post-payment reassurance closes double-pay window + "where did my money go" panic).

---

## MONEY PATH / SIGNUP (agent report, code-verified @ 4bc7975)

Known suspects: (a) activate→onboarding ALREADY FIXED via proxy bounce (`proxy.ts:326-336`; optional 1-line direct redirect) · (b) ?next=/subscribe PARTIAL — `/subscribe` missing from `lib/safe-auth-redirect.ts:9-16` allowlist · (c) dup Stripe customers CONFIRMED (`app/api/billing/new-coach-checkout/route.ts:48` creates unconditionally, no idempotency key) · (d) confirm-email CONFIRMED BROKEN at code level (no verify banner on login; NO `/auth/confirm` handler anywhere — zero `exchangeCodeForSession`/`verifyOtp` matches; VERIFY-IN-PROD whether Confirm-email is ON).

### 🔴 Launch blockers
- **M1. Pay $99 → black hole if redirect lost.** Webhook deliberately no-ops new-coach checkout (`app/api/webhooks/stripe/route.ts:148-152`); activate route requires surviving browser session (`new-coach-activate/route.ts:19-21,77-79`). Charged, no workspace, no retry. Fix: idempotent activation in webhook `checkout.session.completed` (has `user_id` metadata). **M**
- **M2. Activation errors invisible → second-charge invite.** `/subscribe` never reads `?error`/`?cancelled` (`SubscribePageContent.tsx`); user who paid sees pristine $99 button; with (c) each retry = new customer. Fix: error banner. **S**
- **M3. No-profile users hard-locked out of login.** No trigger/code creates profile pre-payment; `app/api/auth/login/route.ts:89-101` 403s coach-intent without profile → signed-up-unpaid coach on new device can never return to pay. Fix: allow coach-intent login when profile absent (→ /subscribe). **S** (also the safety net for (d); signup route's cookie write uses plain `createClient`, `app/api/auth/signup/route.ts:116`, the exact bug login was hardened against per `lib/supabase-server.ts:35-39`)
- **M4. OWNER: verify founding Price = $99/mo recurring LIVE** (`lib/stripe.ts:19` trusts env blindly; UI promises in 5 places) + platform verification cleared.

### 🟡 Fix tonight
- **M5. Founding-spots counter fake** — always "10 of 10": `founding-count/route.ts:21-34` counts with session client but RLS blocks (`20240316000006:39-46`). Fix: `createServiceClient()`. **S**
- **M6. Webhook ACKs fulfillment failures permanently** — event recorded before processing (`webhooks/stripe/route.ts:59-70`), catch returns 200 (`:359-364`), pass/class failures only console.warn (`:83,:90`). Paid → zero credits → no retry, no alert. Fix: 5xx on money-fulfillment failure (+ `logServerError`). **M**
- **M7. Class capacity oversell race** — capacity checked only at checkout create (`book-class/route.ts:109-111`); webhook inserts unconditionally (`lib/stripe-class-booking-webhook.ts:41-57`); dup-paid keeps money with no refund (`:61-63`). Fix: recheck in webhook; refund on reject. **M**
- **M8. Membership double-subscribe race** — guard only at session create (`membership-checkout/route.ts:119-133`); 2 tabs → 2 live subscriptions, webhook inserts 2nd row (`webhooks/stripe/route.ts:126-130`). Also missing `charges_enabled` check (buy-pass has it). Fix: webhook dup check → cancel newer. **M**
- **M9. OWNER DECISION: funnel contradiction + free side door.** Signup promises "No credit card to start" (`SignupPageClient.tsx:378-380`) but funnel demands $99 first; meanwhile `POST /api/auth/signup-complete` creates a free workspace with NO payment check (`signup-complete/route.ts:30-39`), coach layout grants implicit 14-day trial (`app/coach/layout.tsx:72-86`), and once a workspace exists `/subscribe` is unreachable (`proxy.ts:190-193`). Pick paywall-first or trial-first; align copy + routes.

### 💡 Later
- M10. Webhook Stripe API-shape fragility (`invoice.subscription`, `current_period_end` — removed in Basil 2025-03-31); SDK pinned safe but endpoint dashboard version controls payload. VERIFY-IN-PROD endpoint version. · M11. Two divergent subscription gates (`app/coach/layout.tsx:61-87` vs `proxy.ts:337-379`). · M12. `alert()` on billing surfaces. · M13. Spoofable origin fallback in book-class redirect (`book-class/route.ts:395-396`). · M14. Dead signup components + empty route dirs.

**Verified solid:** pass fulfillment idempotent (UNIQUE + 23505 handling); pass-spend/allotment optimistic locks correct; invoice checkout has real idempotency key; webhook dedupe table; pricing copy zero drift; portal/cancel paths wired.

---

## COACH SURFACES (agent report, code-verified)

Lock verdict: route-level Coming-Soon lock AIRTIGHT (all 5 pages + both navs pilled); 2 side-door leaks (C6, C7). Branding clean: zero user-visible Kindo/Sensei under app/coach.

### 🔴 Broken
- **C1. Availability save DELETES class slots (data loss).** `/api/availability` GET returns ALL `recurring_availability` incl. class rows (`app/api/availability/route.ts:24-26`); `AddAvailabilityModal.tsx:63,76-77,184-192` deletes every existing id + re-posts plain windows → classes destroyed, clients can't book. Fix: `.is('class_group_id', null)` filter in GET. **S** VISIBLE
- **C2. Schedule "Send invoice" total no-op.** `SessionDetailDrawer.tsx:493` sets `markPaidOpen`, but modal renders only when `invoiceId` truthy (`:742`) — always null here. Fix: open `QuickInvoiceModal` (exists, used in client detail :567). **S**
- **C3. "Complete session" = window.prompt typing test; "yes" never completes.** `SessionDetailDrawer.tsx:231-261` — prompt "Type: yes, already, or no"; typos abort; `yes` opens RecordPaymentModal and returns BEFORE the status PATCH. Fix: 3-button choice; chain complete after `onRecorded`. **M**
- **C4. Classes page double-renders every new class.** Same root as C1 — class rows pass `bookableSlots` filter (`CoachClassesContent.tsx:129-132`) → legacy table AND ClassScheduleView; legacy trash deletes single weekday of multi-day class (`:391`). Fix: same filter / drop legacy table. **S** VISIBLE
- **C5. Drawer shows ANY pending invoice as this session's; "Resend" opens MarkPaid modal.** `SessionDetailDrawer.tsx:134-142,506-507` — takes `[0]` of client's pending invoices; resend never resends. Fix: match by `session_id`; wire real resend (`InvoicesPageContent.tsx:190`). **M**

### 🟡 Fix tonight
- **C6. Videos "Use in Promote" → Coming-Soon wall** (`VideosPageContent.tsx:472,1628-1637`). Hide when `!isFeatureEnabled('promote')`. **S**
- **C7. Command palette "Go to Promote" unmarked; Passes missing** (`components/CommandPalette.tsx:41,30-49`). **S**
- **C8. Suspended page: "Contact support" with no contact** (`app/coach/suspended/page.tsx:7-9`). **S**
- **C9. Payments raw ISO dates** (`PaymentsPageContent.tsx:480,511`). **S** VISIBLE
- **C10. Leads gating contradicts plan ladder** — "Upgrade to Pro or Scale" (no link, not purchasable; founding INCLUDES Local Scout per `plan-limits.ts:29`); sidebar pills PRO (`CoachSidebarShell.tsx:42`). **S** VISIBLE
- **C11. Memberships+Passes missing "Stripe not connected" warning** (Classes has it: `CoachClassesContent.tsx:225-241`; client checkout 503s). **S** VISIBLE
- **C12. Settings copy: dead dark-mode toggle instructions + "brand sapphire"** (`SettingsPageContent.tsx:1057-1058`). **S** VISIBLE
- **C13. Appearance Light/Dark picker reverts on reload** (`appearance/page.tsx:334-399` vs `ThemeProvider.tsx:19-23` force-dark). Remove picker or honor stored. **S** VISIBLE
- **C14. Raw enums in badges, 6 pages** (schedule ×2, invoices, clients ×2, client-detail, messages; `no_show` renders verbatim). Fix: shared `statusLabel()`. **S** VISIBLE
- **C15. Native dialogs 12+ paths** (worst: delete-client `ClientsPageContent.tsx:246`; prompt in C3; alert() on billing/subscription). Passes/Memberships have the right modal pattern to copy. **M** (prioritize delete-client)
- **C16. More sheet missing PRO pill on Lead Research** (`CoachMoreSheet.tsx:31`). **S**

### 💡 Later
C17 Passes shares Packages icon · C18 palette fake actions/shortcuts · C19 videos empty-state copy contradicts upload · C20 failed-video retry never renders (`onRetry={undefined}` :468) · C21 dead nav components w/ stale Promote//billing links (delete) · C22 /billing vs /coach/subscription drift · **C23 support emails: support@foundos.ai (terms), hello@foundos.ai (subscription+footer), privacy@foundos.ai (privacy), NONE (suspended) — pick ONE canonical** · C24 coach recap upload uses client API route · C25 terminology CLEAN.

**Agent top 5:** C1 → C4 → C2 → C3 → C6+C7.

## LIVE WALKTHROUGH (Playwright, desktop + 375px, demo coach + demo client)

Coach + client logins both work. ~35 routes × 2 viewports captured to `screenshots/audit-2026-06-09/`. Console clean on every page EXCEPT:
- 🔴 **W1. `/api/testimonials` 500 ×4** on Analytics → visible red "Could not load testimonials" at page bottom.
- 🟡 **W2. `405 GET /api/settings/workspace`** ×2 from Classes page (method mismatch — route lacks GET or caller wrong).
- 🟡 **W3. `404 GET /api/coach/classes/{id}/bookings?date=…` ×4** from Classes page — bookings fetch hits dead route (likely id/route mismatch; relates to C1/C4 cluster).
- 💡 **W4. Recharts "width(-1)" warnings ×6** on Analytics (hidden-container sizing).
- ✅ `/client/assignments` redirects to portal (neutralized correctly); ⚠️ `/coach/assignments` still renders a live page (nav-hidden only).
- Visual: Classes page shows BOTH legacy slots section (own empty state + "New class slot" CTA) AND new ClassScheduleView — C4 confirmed visually. Landing: hero/CTA/founding offer on-brand; below-fold voids in screenshots = scroll-reveal artifact (verify "Find a Coach" hero CTA target ≠ mock /browse).

---

## PHASE 2 PLAN (fix batches — parallel agents, disjoint files, I commit per batch)

- **A. Schedule/Classes data-safety:** C1+C4 (availability filter), C2 (Send invoice → QuickInvoiceModal), C3 (mark-complete buttons), C5 (session-scoped invoice + real resend), W2+W3 diagnosis/fix.
- **B. Money server:** M1 (webhook activation fallback), Mc (customer reuse), M5 (founding count), M6 (no-ack fulfillment failures + logServerError), M7 (capacity recheck — conservative: allow + alert, no auto-refund), M8 (membership dup → cancel newer + alert).
- **C. Auth UX:** M2 (subscribe error banner), M3 (no-profile login recovery + verify banner), Mb (/subscribe allowlist), signup cookie-queue fix, honest signup copy (replace false "No credit card to start").
- **D. Client portal:** P1 (booked success), P2 (mobile booking links), P3 (sessions banner), P4 (passes poll), P6 (expired filter), P7 (UTC greeting).
- **E. Coach polish:** C6-C16 S-items, W1 testimonials 500, W4 charts, C19/C20, support email → hello@foundos.ai everywhere (owner: confirm mailbox), landing Find-a-Coach check, audit-doc refresh.
- **Owner gates tonight:** founding price verify (M4) · funnel decision (M9) · perf migration `20260609090000` paste · Supabase Pro/leaked-password · Stripe identity verification.

## PERF ADVISORS DIGEST (agent report — 360 lints parsed)

Counts: `multiple_permissive_policies` 156 WARN · `unused_index` 98 INFO · `auth_rls_initplan` 56 WARN · `unindexed_foreign_keys` 49 INFO · `duplicate_index` 1 WARN (`videos`: `idx_videos_workspace` ≡ `idx_videos_workspace_id`).

- 🟡 **auth_rls_initplan = the #1 Disk-IO multiplier on this Free/Nano DB** (the tier that already had an IO outage). 56 policies across 21 tables re-evaluate `auth.uid()` per ROW; hottest = `messages` (chat polling), `weekly_unavailability` (8 policies), `client_passes`/`coaches`/`workspaces` (4 each). Fix = wrap as `(select auth.uid())` — semantics-preserving. Agent produced an atomic DO-block that rewrites all of them + 7 highest-traffic FK indexes. **OWNER GATE: schema change → staged as a migration for dashboard paste; after running, re-run perf advisors (56→0 expected) + smoke login/portal/booking/messages.**
- ⚠️ Regression pattern: the 06-08 passes migration shipped bare `auth.uid()` RLS while 06-05/06 promote/studio migrations were initplan-clean. Future migrations: always `(select auth.uid())`.
- New tables: `promote_profile` zero lints; `promote_posts`/`scheduled_posts`/`video_projects` INFO-only (FK indexes); `class_passes` + `client_passes` carry the initplan WARNs above.
- 💡 Deferred: merge 156 duplicate permissive policies (auth-semantics change — start with `weekly_unavailability`, `workspaces` triple-SELECT); prune 98 unused indexes after real traffic; remaining 42 FK indexes as features see use.
- Full ready-to-paste SQL: staged migration (see Phase 2) — source = perf-digest agent report.
